<h2>固定費科目の詳細</h2>

<%= link_to "編集", [:edit, @fixcedcost] %>
<%= link_to "固定費科目一覧へ戻る", :fixcedcosts %>

<table>
   
    <tr>
    <th>名称</th>
    <td><%= @fixcedcost.name %></td>
    </tr>
    
    <tr>
        <th>説明</th>
        <td><%= @fixcedcost.description %></td>
    </tr>
</table>
